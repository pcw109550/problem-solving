#include <bits/stdc++.h>
using namespace std;
enum plane {U, D, F, B, L, R};
enum color {w, y, r, o, g, b};
enum dir { CLWR, CCLWR };



class Cube {
    public:
        int id;
        Cube(int id) : id(id) { };
        int state;
};



int main(void) {
    int T; cin >> T;
    //TODO
}