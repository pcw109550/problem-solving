#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0); cin.tie(0);
    int T, N; cin >> T;
    for (int t = 0; t < T; t++) {
        cin >> N; cout << (int) sqrt(N) << '\n';
    }
}