#include <bits/stdc++.h>
using namespace std;
int main(void) {
    int T; cin >> T;
    for (int i = 0; i < T; i++) {
        int N; cin >> N; int input[N] = {}; bool resolved[N] = {};
        int result = 0;
        for (int j = 0; j < N; j++) {cin >> input[i]; input[i]--; }
        for (int j = 0; j < N; j++) {
            if (resolved[j]) { continue; }
            stack<int> S; int start = j;
            

        }
    }
}